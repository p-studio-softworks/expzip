# Expzip の作業の決まり

Windows 向けのアーカイバ。C# / .NET 10 / WPF、配布物は単一の `Expzip.exe`。
詳しい仕様は `docs/SPEC.md`。ここには**守る決まりだけ**を書く。経緯は書かない。

## ビルドとテスト

- ビルドは `build.cmd`(`-Debug` 開発用 / `-Test` 画面テストも走らせる)。`dotnet publish` を直に叩かない
- 画面テストは `tests/ui/Run-UiTests.ps1`。実際に Expzip を起動して操作するので、走行中は入力に触らない
- **画面の文言を変えたら `tests/ui/suites` の期待値も直す。**文言はテストで見ている
- **文言を変えたら `dotnet run --project tools/stringdump` で一覧を作り直してコミットする。**
  `tools/stringdump/strings.txt` の差分を見れば、直すつもりの無かった文言まで変わっていないかが分かる
  (`-- --check` でずれていないかだけ見られる)

## ファイルの形式

- `.cs` `.xaml` `.csproj` `.ps1` `.md`: UTF-8 + CRLF。`.ps1` と `.cs` は BOM 付き(PowerShell 5.1 は BOM が無いと日本語を読み違える)
- `.cmd` は ASCII のみ(cmd.exe が ANSI で読むため、日本語は `.ps1` 側に置く)
- 一括置換をしても BOM と改行コードを壊さない

## 画面の色

- **`Style` を書くときは、`BasedOn` で Fluent のデフォルトを継承する。**継承しないと部品は古い見た目に戻り、暗いテーマでも明るい背景色のまま残る。列のある一覧で継承するのは `GridViewItemContainerStyleKey`
- **`ControlTemplate` を書いたら、文字色も書く。**書かないと古いデフォルトの黒が残り、暗い背景色では消える
- 色の決まりは `docs/SPEC.md` の 4.9章

## 画面に出る文言

- 文言は `Localization/Strings.Text.cs` の表に日本語と英語を並べて持つ。**XAML には書かない**
- 操作の言葉は漢字にする(入力する、設定する、削除する、作成する、選択する、使用する、展開する)
- 開発者の言葉を使わない(断片 → 分割ファイル、置き場 → 保存先)。**「建てる」ではなく「ビルド」**
- 数と単位の間に半角空白(`3 件`)。ファイルや項目は「個」、ルール・違反・検出は「件」
- `フォルダー` `ヘッダー` と伸ばす。`既に` は漢字
- ツールチップとステータスバーは「。」で終えない。ダイアログの文は終える
- 内部の理由は書かない。**ただし安全に関わる注意は残す**
- 失敗の理由は `Strings.Reason` を通して日本語で出す。英語の例外文をそのまま出さない

## 名義

- 著作権表示は `Copyright (c) <年> P studio`。**個人名は出さない**
- コミットの名義は `paliensup <193758124+paliensup@users.noreply.github.com>`(このリポジトリ限定の設定)
- **コミットに `Co-Authored-By` を付けない。**付けると GitHub で連名として表示される

## 書きもの

- `README.md` は**利用者向け**。開発者向けの手順は書かない
- **「色弱」「色盲」とは書かない。**「色の見え方の違い」「色覚特性」と書き、型を言うときは
  「赤と緑の区別がつきにくい」のように見え方で言う。チケットやコミットメッセージも同じ
- 仕様書 `docs/SPEC.md` は「いまどうなっているか」。**段取り(フェーズ)は書かない**
- **課題管理は非公開。リポジトリからそこへリンクしない**(`#123` という番号だけなら書いてよい)
- **ほかのアーカイバを、比較先や由来として書かない**(#127)。比べる相手が要るときは
  エクスプローラーや 7-Zip にする。例外は README の、関係がないと断る 1 行だけ。
  詳しくは課題管理の #127

## 進め方

- ファイルの書き換えは Edit / Write で行う。**外部スクリプトで書き換えると、そのファイル全体が読み直されて無駄が大きい**
- ログや出力は必要な行だけ見る(`Select-String 'NG'` など)
